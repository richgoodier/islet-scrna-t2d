EDGER - Gene quantification

srun --pty --partition=courses --export=ALL --mem=16G -t 4:00:00 bash
conda activate project
module load R
R

1. Install edgeR library
if (!requireNamespace("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install(“edgeR")

2. Load edgeR by -> library(edgeR)
library(edgeR)

3. counts <- read.table("/scratch/goodier.r/counts_combined.txt", header = TRUE, row.names = 1)

4. gene_lengths <- counts$Length

5. count_matrix <- counts[, !(colnames(counts) %in% c("Geneid", "Chr", "Start", "End", "Strand", "Length"))] # Select only count columns

6. y <- DGEList(counts = count_matrix)
rpkm_values <- rpkm(y, gene.length = gene_lengths)

7. write.table(rpkm_values, "rpkm_combined.txt", sep = "\t", quote = FALSE)

8. q()